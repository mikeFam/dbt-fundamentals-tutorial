version: 2

models:
  - name: dim_customers
    description: customer dimension
    columns:
      - name: CUSTOMER_ID
        descriptions: primary key
        tests:
          - unique
          - not_null
          - relationships:
              to: ref('fct_orders')
              field: customer_id
          
  - name: fct_orders
    description: orders facts table
    columns:
      - name: CUSTOMER_ID
        descriptions: primary key